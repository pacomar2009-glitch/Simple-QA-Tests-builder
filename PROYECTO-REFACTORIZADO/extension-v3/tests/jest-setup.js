/**
 * Jest Setup para ES Modules
 * Configura globales necesarias para tests
 */

// Importar jest globals para ES modules
import { jest } from '@jest/globals';

// Exportar jest como global
global.jest = jest;
